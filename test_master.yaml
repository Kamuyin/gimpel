listen_address: ":9090"
data_dir: "./test_data/master"

registration_tokens:
  - "test-token-12345"

ca:
  auto_generate: true
  organization: "Gimpel Test"
  validity_days: 30

registry:
  stale_timeout: 30s
  cleanup_interval: 10s

sandbox:
  nodes:
    - "sandbox-1.local"
    - "sandbox-2.local"
